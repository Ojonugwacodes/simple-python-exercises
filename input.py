# Simple program that prompts a user to input a name.
name = input("What is your name?")
# "Name" is stored as a variable and then the program prints a welcome message using "name" as a return value
print(f"Welcome, {name}")
